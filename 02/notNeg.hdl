/** Helper chip for negative 16th bitcalculation.
*
* Finds if last bit of input 16 bit word is 0 or 1
*/

CHIP notNeg {
    IN
    	a[16];
    OUT
    	out;
    
    PARTS:
   And(a=a[15],b=true,out=out);
  }
